# Supabase (required - keeps app connected to database)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App config
ADMIN_EMAILS=
NEXT_PUBLIC_SITE_URL=
CRON_SECRET=

# All integration credentials (Amazon, QuickBooks, Shopify, Amplifier)
# are stored in the integration_credentials table in Supabase.
# Manage them via the Connections page in the dashboard.
